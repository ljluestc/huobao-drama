## 摘要
- 问题：Issue #84 反馈“当某一个分镜视频生成失败后，后续无法继续合成与导出”。  
- 现象：`/api/v1/episodes/:id/finalize` 返回成功，但后台异步 merge 因单个素材文件不存在报错：  
  `ffmpeg merge failed: failed to download clip 0: local file not found: data/storage/videos/...mp4`  
- 影响：只要队列中有一个无效素材，整集 merge 任务失败，且后续导出流程被阻塞，用户必须手工排障。

## 关联 Issue
- Closes #84

## 问题分析
从日志看，当前流程在构建 merge 输入时默认“所有分镜素材都可用”，缺少对本地文件存在性的前置校验与降级策略：

1. 分镜层存在“生成记录”，但对应本地文件已不存在（路径失效/清理/挂载变化）。  
2. merge 任务遇到首个无效素材直接失败，且错误会阻断当前整集合成。  
3. 导出依赖 merge 成功产物，导致“后续无法再合成和导出”的连锁失败体验。

## 变更方案
### 1) Merge 前增加素材可用性校验
- 对进入 merge 的每个 clip 执行统一校验：
  - 路径非空
  - 本地文件存在（`fs.existsSync` / stat）
  - 时长可读（可选，ffprobe 兜底）
- 将校验结果写入结构化日志（scene_id / storyboard_id / local_path / reason）。

### 2) 单 clip 失败不再阻断整集（可恢复执行）
- 对无效 clip 使用“跳过并继续”的策略（默认），并统计 `skipped_count`。  
- 当有效 clip 数量为 0 时，merge 任务才标记为失败并返回明确错误。
- 当有效 clip 数量 > 0 时，允许继续合成并产出可导出视频，同时记录告警。

### 3) 状态机与错误语义优化
- `video_merge` 的 `error_msg` 增强为“可读 + 可定位”格式（包含缺失文件与场景编号）。  
- merge 失败后不锁死后续流程：支持用户修复素材后直接重试 merge。  
- 导出入口给出明确提示：
  - “存在跳过片段，已导出可用版本”
  - 或“无可用片段，无法导出”

### 4) 任务幂等与重试
- 同一 episode 的重复 finalize/merge 请求做幂等保护（避免并发重复任务互相覆盖状态）。
- 提供显式 retry 逻辑：重试前清理旧临时文件与中间状态，避免脏数据影响下一次合成。

## 用户可见变化
- 单个分镜素材损坏时，不再导致整集流程完全中断。  
- 合成与导出可在“部分可用”条件下继续进行。  
- UI/日志会明确提示哪些分镜被跳过，方便用户定点补图或补视频后再重试。

## 验证方案
### 自动化测试（建议）
1. **存在缺失素材 + 存在有效素材**  
   - 期望：merge 成功、`skipped_count > 0`、可导出。
2. **全部素材缺失**  
   - 期望：merge 失败，返回“无可用片段”错误。
3. **修复后重试**  
   - 期望：merge 可恢复成功，状态从 failed -> completed。
4. **并发 finalize/merge**  
   - 期望：仅一个有效执行，其他请求复用任务或返回幂等结果。

### 手工回归（建议）
- 模拟删除 `data/storage/videos/*.mp4` 中某一条目后触发 finalize。  
- 验证导出页面可继续导出可用版本，并在界面看到“片段被跳过”提示。  
- 补回缺失素材后重试，验证最终完整导出成功。

## 风险与缓解
- 风险：默认“跳过缺失片段”会改变历史严格失败语义。  
- 缓解：
  - 增加配置项（严格模式/容错模式）；
  - 默认容错并在日志和 UI 中强提示；
  - 对关键场景（如首尾镜头）可设为“不可跳过”策略（可后续增强）。

## 兼容性
- 向后兼容：是（新增容错逻辑，不破坏现有数据结构）。  
- 数据迁移：无强制迁移。  
- 配置变更：可选新增 merge 容错开关（如 `merge.skip_missing_clips`）。

## Release Note
修复视频合成链路：当单个分镜视频缺失或生成失败时，merge 任务不再直接阻断后续导出流程，支持跳过异常片段并继续生成可用成片，同时提供可重试与明确错误提示。

## 备注
- 当前为中文 PR 描述草稿，已按 issue #84 场景整理，可直接作为 PR 正文使用。
