## 摘要
- 问题：Issue #92 提出“音频生成任务应改为可选”，因为当前主流视频模型已支持音视频同步生成，不再需要每次都额外执行独立音频任务。
- 影响：现有强制音频流程会增加任务时长与资源成本，并在不需要独立配音时造成冗余步骤。
- 方案：将“音频生成”从默认必选改为可选流程，通过配置/参数开关控制是否创建与执行音频任务；默认走“视频模型内建音频”路径。

## 关联 Issue
- Closes #92

## 变更说明
### 1) 任务编排层：音频任务可选化
- 将原本在视频生成后“固定追加音频任务”的逻辑改为条件触发。
- 新增 `enableAudioTask`（命名可按项目现有规范调整）控制开关：
  - `false`：跳过独立音频任务（默认推荐）
  - `true`：保留并执行独立音频任务

### 2) 配置与接口层：显式控制策略
- 在项目配置（全局默认）中提供“是否启用独立音频任务”选项。
- 在单次生成接口/工作流参数中允许覆盖全局默认，满足不同项目的精细化需求。
- 配置优先级建议：
  1. 单次任务参数
  2. 项目级配置
  3. 系统默认值

### 3) 前端交互层：展示与提示同步
- 在创建/执行视频生成任务的入口增加“独立音频任务”开关（默认关闭）。
- 当开关关闭时，界面提示“将使用视频模型内建音频能力，不再单独生成配音任务”。
- 当开关开启时，保持原有音频任务状态展示与结果回填。

### 4) 后端兼容层：老任务与老配置兜底
- 对历史任务数据做向后兼容：未显式配置时按新默认处理（跳过独立音频任务）。
- 保留旧字段读取能力，避免因字段缺失导致任务失败。

## 用户可见行为变化
- 默认情况下，用户发起视频生成后不会再自动触发独立音频任务。
- 若用户明确需要单独配音/后期音频替换，可手动开启“独立音频任务”。
- 对使用支持音频同步输出的视频模型的用户，整体流程更短、资源消耗更低。

## 验证结果
- 功能验证（建议）：
  - 开关关闭：仅生成视频任务，不创建音频任务记录。
  - 开关开启：视频任务后正常创建并执行音频任务。
  - 旧项目/旧配置：任务可正常执行，不报字段缺失错误。
- 回归验证（建议）：
  - 不支持音频同步的视频模型下，开启独立音频任务仍可正常工作。
  - 任务状态流转与日志记录保持一致性。

## 风险与缓解
- 风险等级：中低。
- 风险点：
  - 不同模型能力不一致，默认跳过独立音频可能导致部分模型缺少最终音轨；
  - 前后端开关语义不一致可能导致行为偏差。
- 缓解措施：
  - 对模型能力做白名单/能力标识判断；
  - 增加统一配置解析层与端到端用例，确保开关语义一致；
  - 在 UI 明确提示当前模型是否支持音频同步输出。

## 兼容性
- 向后兼容：是
- 数据迁移：无强制迁移（建议补充可选迁移脚本用于批量设置默认策略）
- 配置变更：新增可选开关，未配置时使用安全默认值

## 备注
- 当前工作保持在私有分支：`private/issue-92-optional-audio-task`
- 按要求仅准备本地 PR 描述并推送到私有分支，不在本步骤创建 GitHub PR。
